# ── Core numerical stack ──────────────────────────────────────────────────────
numpy>=1.24,<2.0
scipy>=1.11,<2.0

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-contrib-python>=4.8,<5.0   # contrib for extra features (SIFT fallback etc.)

# ── Visualization ─────────────────────────────────────────────────────────────
open3d>=0.18
matplotlib>=3.7

# ── Pose Graph Optimization (layered: try in order) ──────────────────────────
# Option 1: g2o Python bindings (may fail to build; handled gracefully)
# g2o-python>=0.0.11
# Option 2: pure-Python SE3 graph SLAM (always available)
graphslam>=0.4.0

# ── Evaluation ────────────────────────────────────────────────────────────────
evo>=1.28          # ATE / RPE evaluation + KITTI trajectory tools

# ── Math / symbolic ───────────────────────────────────────────────────────────
sympy>=1.12        # for math_derivations notebook

# ── Jupyter ───────────────────────────────────────────────────────────────────
jupyter>=1.0
notebook>=7.0
ipykernel>=6.0
ipywidgets>=8.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65
pyyaml>=6.0
rich>=13.0         # pretty console output
click>=8.1         # CLI argument parsing

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=7.4
pytest-cov>=4.1
